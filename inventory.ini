# Este fichero se genera automáticamente al ejecutar: cd terraform && terraform apply
# No editar a mano — cualquier cambio será sobreescrito por Terraform.
#
# Si ves este mensaje y quieres desplegar, sigue los pasos del README_NUEVO.md:
#   1. cd terraform && terraform init && terraform apply
#   2. cd .. && ansible-playbook -i inventory.ini playbook.yml
